node_modules
lib

# Yarn non zero-install. See: https://yarnpkg.com/getting-started/qa#which-files-should-be-gitignored
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks

# We are using the `version` plugin just to do a bump commit.
# We don't need the entire https://yarnpkg.com/features/release-workflow feature set.
.yarn/versions
